import 'package:flutter/material.dart';

class AppWebLayout extends StatelessWidget {
  const AppWebLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('AppWebLayout'),
      ),
    );
  }
}
